name: Scheduled Ping

on:
  schedule:
    - cron: '0 */2 * * *'  # Runs every 2 hours

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Website
        run: curl -sS "https://www.dondelodan.com/" > /dev/null
